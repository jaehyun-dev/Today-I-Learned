class User:  # 클래스 이름 첫 글자는 항상 대문자
    # 클래스의 내용 작성
    # 클래스의 내용에는 속성과 행동 들어감
    pass    # 아무 내용 없다는 뜻. 클래스의 내용을 비워두면 에러가 발생하기 때문에.


user1 = User()  # User 인스턴스를 만들어지고 user1이 이 인스턴스를 가리키게 됨
# 위에서 정의한 User 클래스를 이용해서 User 인스턴스를 만든 것
user2 = User()
user3 = User()

# User 인스턴스 3개를 만들었음. 세 인스턴스는 모두 서로 다른 존재.
# 같은 클래스로 만들었어도 서로 다른 인스턴스